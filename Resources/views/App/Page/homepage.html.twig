{% extends 'ParabolAdminCoreBundle::App/base.html.twig' %}

{% block content %}

	{{ app.request.query.get('msg') }}

	{{ page is empty ? 'You need to add hompage to database' : page.content | raw }}

	{{ render(controller('ParabolAdminCoreBundle:Page:test', {}, {dev: 'alalala'})) }}	
{% endblock %}